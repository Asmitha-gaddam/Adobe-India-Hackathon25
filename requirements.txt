# requirements.txt

# PDF extraction
PyMuPDF==1.22.0         # For fast and layout-aware text extraction using fitz

# Embedding + similarity
sentence-transformers==2.2.2
huggingface-hub==0.13.4
scikit-learn==1.3.0     # For cosine similarity
numpy==1.24.0

# (Optional but useful)
tqdm==4.66.1            # For progress bars during processing
